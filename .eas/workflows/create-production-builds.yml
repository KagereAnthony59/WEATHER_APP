name: Automated Android Preview Builds

on:
  push:
    branches:
      - main

jobs:
  build_android:
    name: Build Android APK (Preview)
    type: build
    params:
      platform: android
      profile: preview
